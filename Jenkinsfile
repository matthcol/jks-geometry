pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo 'compile'
            }
        }
        stage('package') {
            steps {
                echo 'package'
            }
        }
    }
}